import { SampleData } from "../SynthEvent";
type Sample = SampleData<Float32Array>;
export type SampleTableItem = Sample & {
    velRange: [number, number];
};
export declare class SampleTable {
    private samples;
    addSample(sample: Sample, bank: number, instrument: number, keyRange: [number, number], velRange: [number, number]): void;
    getSamples(bank: number, instrument: number, pitch: number, velocity: number): Sample[];
}
export {};
