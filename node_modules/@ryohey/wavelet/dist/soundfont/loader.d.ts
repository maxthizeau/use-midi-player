import { SampleData } from "../SynthEvent";
export type SoundFontSample = SampleData<ArrayBuffer> & {
    bank: number;
    instrument: number;
    keyRange: [number, number];
    velRange: [number, number];
};
export interface BufferCreator {
    createBuffer(numberOfChannels: number, length: number, sampleRate: number): AudioBuffer;
}
export declare const getSamplesFromSoundFont: (data: Uint8Array, ctx: BufferCreator) => import("../SynthEvent").LoadSampleEvent[];
